name: secret-test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Use secret
        run: |
          echo "Secret exists"
          test -n "$TOKEN"
        env:
          TOKEN: ${{ secrets.DEMO_TOKEN }}
